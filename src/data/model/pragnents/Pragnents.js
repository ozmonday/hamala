export const LOADDATA = 'loadData'



const initPragnents = []

export const pragnentReducer = (state = initPragnents, action) => {
    switch (action.type) {
        case LOADDATA:
            
            break;
    
        default:
            break;
    }
}
